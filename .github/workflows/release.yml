name: Gradle Release

on:
  repository_dispatch:
    types: release

jobs:
  build:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-release.yml@v3.0.0
    permissions:
      contents: write
      packages: write
    with:
      flow-type: hybrid
      java-version: '21'
      commit-hash: ${{ github.event.client_payload.commit }}
      build-version: ${{ github.event.client_payload.project_version }}
      # Central refuses a version it already holds, so a half-published release can only be
      # finished by resuming — never by re-dispatching.
      resume-deployment-id: ${{ github.event.client_payload.resume_deployment_id }}
      docker-image: reporting-service
      skip-extra-tasks: ft
      github-packages-publications: ":reporting-automation:maven"
    secrets: inherit
